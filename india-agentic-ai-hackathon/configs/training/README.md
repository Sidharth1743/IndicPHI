# Training configs

Stub for downstream GLiNER fine-tune and learning-curve experiments
(Track C extrinsic evaluation). See `gliner.yaml`.

Training code (`main/train/gliner_finetune.py`) is planned and not required
for the smoke demo.
